This directory contains scripts which were previously useful but are no longer actively used.  

These scripts are left around for reference reasons and because they may be useful again.  However, because changes have taken place to the rest of the media cloud code, we make no guarantees that these scripts will run correctly or even that they will compile.