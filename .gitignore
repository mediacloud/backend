data.old
matrix
mediawords.yml
mediacloud.tmproj
jons_stuff
.carton
local
cache/translate

# Perl::Tidy backup files
*.pm.bak
*.t.bak

# OS X
.DS_Store
*.pl.bak

# Inline::Java
_Inline/

# carton.lock doesn't carry any important information since Carton 0.9.x; also,
# it doesn't seem to be compatible between Ubuntu and OS X versions
carton.lock
