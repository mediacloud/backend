[% title = 'Tags for Tag Set' _ tag_set.name | html %]
[% INCLUDE include/header.tt2 %]

<fieldset>
    <a href="[% c.uri_for( '/search/tag_sets' ) %]">List Tag Sets</a>
</fieldset>
<table>

    <tr>
        <th>ID</th>
        <th>Tag</th>
        <th>Media Sets</th>
        <th>Num Media</th>
        <th>List Media</th>
    </tr>

    [% FOREACH tag IN tags -%]

        <tr [% IF even %]class="even"[% END %]>
            <td>[% tag.tags_id %]</td>
            <td>[% tag.tag | html %]</td>
            <td>[% tag.media_set_names | html %]</td>
            <td>[% tag.media_count %]</td>
            <td><a href="[% c.uri_for( '/search/media/' _ tag.tags_id ) %]">list media</a></td>
        </tr>

    [%  SET even = !even %]

    [% END # FOREACH medium -%]

</table>


[% INCLUDE include/footer.tt2 %]
