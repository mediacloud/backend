auto_fieldset:

elements:
  - type: Text
    name: pattern
    label: Pattern
    comment: Regex to apply to each story sentence within the query
    constraints:
        - Required
        
  - type: Submit
    value: queue search
