auto_fieldset:

elements:
  - type: Text
    name: name
    id: name
    label: Name
    comment: Name for the focus
    constraints:
        - Required

  - type: Text
    name: query
    id: query
    label: Query
    comment: Solr query to run against the topic
    constraints:
        - Required

  - type: Submit
    value: Go
